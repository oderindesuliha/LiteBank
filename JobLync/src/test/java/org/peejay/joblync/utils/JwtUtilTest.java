package org.peejay.joblync.utils;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
@ActiveProfiles("test")
public class JwtUtilTest {

    // This is an integration test that would require the Spring context
    // For unit testing, you would typically mock the dependencies
    
    @Test
    public void contextLoads() {
        // This test ensures the Spring context loads properly with the JwtUtil
    }
}